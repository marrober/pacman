apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: build-base-image
spec:
  params:
    - name: quay-io-account
      description: The quay.io account that matches the credentials stored in the mounted secret.
      type: string
      default: ""
    - name: quay-io-repository
      description: The quay.io repository in which to store the image.
      type: string
      default: ""
    - name: quay-io-image-tag-name
      description: The tag to use to identify the image.
      type: string
      default: "latest"
    - name: TLSVERIFY
      description: Verify the TLS on the registry endpoint
      type: string
      default: 'false'
    - name: STORAGE_DRIVER
      description: The Buildah storage STORAGE_DRIVER
      type: string
    - name: path
      type: string
  steps:
    - name: build-base-image
      command:
        - buildah
        - bud
        - '--tls-verify=$(params.TLSVERIFY)'
        - '--storage-driver=$(params.STORAGE_DRIVER)'
        - '--root'
        - '/files/buildah-containers'
        - '--layers'
        - '-f'
        - /$(workspaces.files.path)/$(params.path)/base-image/04-dockerfile/fix-open-liberty-image
        - '-t'
        - quay.io/$(params.quay-io-account)/$(params.quay-io-repository):$(params.quay-io-image-tag-name)
        - .
      image: image-registry.openshift-image-registry.svc:5000/myapp-ci/buildah:latest
      securityContext:
        capabilities:
          add: ["SETFCAP"]
      workingDir: /$(workspaces.files.path)/$(params.path)
      resources:
        limits:
          cpu: '1'
          memory: 500Mi
        requests:
          cpu: 250m
          memory: 100Mi
  volumes:
    - name: quay-auth-secret
      secret:
        secretName: quay-auth-secret
  workspaces:
  - name: files
    mountPath: /files
